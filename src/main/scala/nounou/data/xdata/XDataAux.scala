package nounou.data

/**This trait is a simple marker Trait for XData, which indicates that the data is auxilliary.
  * This means, for instance for imaging data, parallel continuous data such as stimulus signals,
  * which are simulatneously digitized.
 * @author ktakagaki
 * @date 2/25/14.
 */
trait XDataAux extends XData
